<%= search_form_for @q do |f| %>
  <%= f.label 'Find a restaurant' %>
  <%= f.text_field :name_cont %>
  <%= f.submit 'Search' %>
<% end %>

<% if @restaurants.count > 0 %>
  <%= pluralize(@restaurants.count, 'restaurant found') %>
<% end %>